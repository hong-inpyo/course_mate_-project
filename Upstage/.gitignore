# ── API 키 (절대 커밋 금지) ──
secrets.json
.env

# ── 로그 ──
*.log

# ── 파이썬 ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── 재생성 가능한 캐시 / 벡터DB ──
# 코드로부터 자동 생성되는 산출물이라, 팀원마다 로컬에서 새로 빌드하면 됨.
# 바이너리라 git으로 관리하면 매번 충돌만 남.
chroma_db/
poc_cache/
cache/chroma_db/
cache/poc_cache/
cache/output/
output/

# ── 에디터 설정 (팀원마다 다를 수 있어 보통 제외) ──
.vscode/

# ── OS 잡파일 ──
.DS_Store
Thumbs.db
